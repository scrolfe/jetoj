<h1>Posts#index</h1>

<% @posts.each do |post| %>
  <p><%= image_tag post.image.url %></p>
  <p><%= image_tag post.image.url(:medium) %></p>
  <p><%= image_tag post.image.url(:thumb) %></p>
  <h2><%= post.title %></h2>
  <p><%= post.body %></p>
<% end %>
